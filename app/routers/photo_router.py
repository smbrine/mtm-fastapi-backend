from fastapi import APIRouter

router = APIRouter(prefix="/photo", tags=["photo"])

@router.post("/add")
async def add_photo():
    pass
from fastapi import APIRouter

router = APIRouter(prefix="/photo", tags=["photo"])

@router.post("/add")
async def add_photo():
    pass
from fastapi import APIRouter

router = APIRouter(prefix="/photo", tags=["photo"])

@router.post("/add")
async def add_photo():
    pass
from fastapi import APIRouter

router = APIRouter(prefix="/photo", tags=["photo"])

@router.post("/add")
async def add_photo():
    pass
from fastapi import APIRouter

router = APIRouter(prefix="/photo", tags=["photo"])

@router.post("/add")
async def add_photo():
    pass
